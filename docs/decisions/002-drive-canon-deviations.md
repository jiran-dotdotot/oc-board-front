# 002 — 자료실(/drive) 정본 편차 원장

- 상태: 확정
- 날짜: 2026-09-07
- 범위: `src/components/drive/**`, `src/hooks/useDrive*.ts`, `src/utils/drive*.ts`
- 계기: 자료실 4단계 재구현 후 적대적 검토(`/adv`, 14대 편성)에서 정본 위반·근거 없는 발명값으로 지적된 항목들.
  코드 주석에만 묻지 말고 원장에 올린다는 CLAUDE.md 「Assumption ledger — surface, don't bury」 규칙에 따른다.

## 정본이 무엇인지

- **디자인 정본**: DesignSync 프로젝트 `1384f01c-020e-4f3b-bd5a-0ca5a21efdaf`
  — 웹 `개선안 통합 앱.dc.html`, 모바일 `개선안 통합 앱 mobile.dc.html`
- **동작 정본**: `../jupiter-board-web` (Vue3 + Pinia)
- **색·치수 정본**: `docs/guides/design-tokens-guide.md` (아트보드 인라인 값보다 이 표가 이긴다)

---

## 1. 「최근 자료」에 정렬·개수 드롭다운을 남긴다

**정본에는 없다.** `isRecentFiles` 블록(웹 `:297-329` · 모바일 `:254-286`)에 `data-dd` 드롭다운
앵커가 0건이고, `dvSortOpts`/`dvPerOpts` 는 `isDrive` 블록(`:858`,`:868`)에만 있다. 정본의 최근 자료
헤더는 `최근 자료 · NEW · {{rfCount}}개 · 일괄 다운로드` 뿐이다.

**그럼에도 남긴다.** 근거:
- 레거시 `src/pages/drive/recent.vue:34-47` 에 정렬 3종이, `:362` 부근에 개수 드롭다운이 실재한다.
- 최근 30일 자료는 수백 건이 될 수 있다. 정렬·페이지 크기 없이는 훑을 수 없다.
- 정본은 이 화면을 얕게만 그렸다 — 같은 문서가 업로드를 「가짜 업로드 데모」라고 자기 주석에 적어 두었다.

**주의**: 「정본에 있다」가 근거가 아니다. 정본에 **없지만** 레거시 근거로 의도적으로 남긴 것이다.

## 2. 「최근 자료」 행에 북마크 버튼을 남긴다

**정본에는 없다.** `aria-label="자료 북마크"` 는 `isDrive` 블록에만 있고, 정본 최근 자료의 행 액션은
`f.preview` · `f.download` 둘뿐이다.

**그럼에도 남긴다.** 레거시 `src/components/drive/DataView.vue:353` 의 `isAvailableBookmark` 가
`/drive/recent` 를 포함한다(home·search 에서만 숨긴다). 자주 쓰는 파일을 최근 목록에서 바로
즐겨찾기하는 동선을 없앨 이유가 없다.

## 3. 업로드를 전체화면 모달로 만든다

**정본에는 업로드 모달이 없다.** 정본은 표 행 안에서 진행률만 보여 준다
(`f.showBar` / `f.pctLabel` / `f.isDone`). 다만 정본 주석이 「업로드 = 가짜 업로드 데모」라고
스스로 밝히고 있어, **정본이 업로드 흐름을 설계하지 않았다**고 읽는다.

**모달로 간다.** 레거시 `UploadDataModal.vue`(900px) 와 같은 구조다. 모달이 주는 것:
등록 «전에» 목록에서 파일을 빼는 스테이징, 드래그앤드롭 영역, 파일별 실패 사유, 재등록(성공분 제외).
이건 정본의 인라인 진행률로는 담을 수 없다.

**따라오는 결과**: 모달이 스크림으로 표를 덮으므로 «목록 행 안의 진행률»은 만들지 않았다.
승인된 플랜에 「모달의 행 + 목록 행 둘 다」로 적어 두었던 것은 모순이었다.

## 4. 저장 용량 게이지 임계 90%

`usagePct >= 90` 이면 막대가 `bg-destructive` 로 바뀐다. **정본·`docs/api/`·정본 토큰표에 근거가 없다.**

**레거시 근거로 유지한다.** `../jupiter-board-web/src/pages/drive/[driveId]/index.vue:293-309`
가 사용량 ≥90% 에서 막대를 빨강으로 바꾼다. 용량이 차 가는 것을 업로드가 실패하기 전에 알리는 실익이 있다.
값을 바꿀 때는 이 문서와 레거시를 함께 고친다.

## 5. 미리보기는 이미지·동영상·PDF 만 (한글·오피스 회귀)

`src/components/drive/preview.ts` 의 `previewKind()` 는 이미지·동영상·PDF 만 열고 나머지는
「미리보기를 지원하지 않는 형식입니다」 안내로 보낸다.

**레거시 대비 회귀다.** `DrivePreviewModal.vue` 는 HWP(자체 뷰어)·오피스(Google gview iframe)·
HEIC(변환 후 표시)까지 창 안에서 열어 준다. 한국어 사내 게시판에서 HWP 는 주력 포맷이라 체감이 크다.

**의도된 단계적 축소다.** 사용자 지시: 「미리보기는 가능하게만 만들어 나중에 대대적으로 공사를 할거야 —
이미지·영상·PDF·한글 이렇게 되도록」. 확장 지점은 `previewKind()` 한 곳이며, 형식만 추가하면 붙는다.

**남은 작업**: HWP 뷰어 · 오피스 뷰어 · PDF 전용 뷰어(페이지 이동·확대) · 기존 저장분 HEIC 표시.

## 6. 폴더 항목 수 `N개` 를 표시하지 않는다

정본 폴더 행은 이름 옆에 `{{ fo.n }}개` 를 보여 준다. **표시하지 않는다** — 서버가 폴더별 파일 수를
주지 않는다. `child_drive_folders` 로 «하위 폴더» 수는 셀 수 있지만 그것을 「N개」로 쓰면 오해를 부른다.
파일 수를 넣으려면 폴더마다 `GET /drive/file` 을 한 번씩 더 치거나 서버에 필드가 필요하다.
사용자 판단: 생략 유지.

## 7. 정렬 옵션 3개 (정본 드롭다운은 4항목)

`new` / `sizeDesc` / `sizeAsc` — 레거시 `[driveId]/index.vue:46-59` 와 동일.
정본 드롭다운은 항목이 4개로 보이지만 네 번째의 실제 라벨을 확인하지 못했고,
`sort[by]` 는 `drive.drive_files` 실제 컬럼이어야 무시되지 않는다(`docs/api/09-drive-file.md` §1).
사용자 판단: 레거시 파리티로 3개 유지.

## 8. 빈 상태 카드 라운드는 12px (아트보드는 8px)

정본 아트보드의 빈 상태 카드 인라인 값은 `border-radius:8px` 이지만 `rounded-lg`(12px)를 쓴다.
CLAUDE.md 가 「색·치수 정본은 `docs/guides/design-tokens-guide.md` 하나뿐 · 아트보드 인라인 값은
정규화 대상」이라고 정하고, 그 표(`:141`)가 **「뱃지 4 · 컨트롤 8 · 카드 12」** 다. 8 은 컨트롤 값이다.

## 9. 그 밖의 발명값

| 값 | 위치 | 근거 |
|---|---|---|
| 동시 다운로드 6개 | `src/utils/driveDownload.ts` `DOWNLOAD_CONCURRENCY` | 없음 — 브라우저 커넥션 한계에 맞춘 값. 레거시는 무제한 병렬(50개 선택 시 50요청) |
| `localStorage['lastDriveBoard']` | `src/components/drive/constants.ts` | 없음 — `?b=` 없는 `/drive` 진입 시 어느 자료실을 열지 정해야 해서 신설. 레거시엔 board 없는 자료실 화면 자체가 없다 |
| 폴더명 30자 | `FOLDER_NAME_MAX` | 레거시 `maxlength="30"` — 서버엔 길이 제한이 없다 |
| 다운로드 취소를 «실패»로 처리하지 않음 | `src/hooks/useDriveDownload.ts` | 레거시 버그를 의도적으로 안 옮겼다 — `abort()` 가 `Promise.all` 을 reject 시켜 「다운로드 실패」 알럿이 뜬다 |
| 폴더 다건 삭제 전 자식 가드 | `src/utils/driveFolders.ts` | `docs/api/08-drive-folder.md:286` 이 프론트 가드를 명시적으로 요구한다. 레거시엔 없어서 고아 폴더가 생길 수 있었다 |

---

## 10. 2회차 검토에서 «사실오류»로 판정한 지적

검토가 올렸으나 근거를 확인한 결과 사실이 아니거나 이미 처리된 것. 재발 방지용으로 남긴다.

| 지적 | 판정 |
|---|---|
| 「최근 자료에 기간 제한 표시가 없다」 | **사실오류.** `DriveScreen.tsx` 가 `recent-files-desc`(「최근 {{n}}일(환경 설정 · 메인화면 기준)간 등록·업데이트된 자료를 보여줍니다.」)를 렌더한다. 남는 논점은 「용량순 정렬이 30일 창 안에서만 돈다」인데 그것이 이 화면의 정의이고 레거시도 같다 |
| `presignFailKey('not_enough_drive_capacity')` 가 절대 매칭되지 않는다 | **문서 상충.** `docs/api/10-…:40` 은 이 키가 «번역됨» 이라 하고 `:126` 은 다중 presign 의 원소별 `result.message` 를 «키 문자열 그대로» 로 적었다. 우리는 다중 엔드포인트만 쓰므로 `:126` 이 적용된다. 번역문이 와도 `default` 분기가 일반 실패 문구로 받는다 — 동작 파손 없음 |
| 모바일 자료실을 무한 스크롤로 (정본은 페이저) | **확정 결정.** 사용자 선택 — 레거시·디자인 설명문서·이 리포의 게시판 목록이 모두 «데스크톱 페이지네이션 + 모바일 무한스크롤» 이다 |
| `/board/recent` 표의 '위치' 컬럼 | **이전 세션 작업.** 이번 자료실 변경분이 아니다. 별도 판단 대상 |

## 11. 2회차에서 «1회차 수정이 만든» 회귀 (전부 수정)

자기 수정이 새 결함을 만드는 패턴이 실측됐다. 기록해 둔다.

| 회귀 | 원인 | 수정 |
|---|---|---|
| presign `extension: ""` → 묶음 전체 400 | 1회차에 presign 확장자를 «표시용» `fileExtension()` 로 교체 | 용도 분리 — `uploadExtension()`(레거시 규칙, 빈 문자열 불가) / `fileExtension()`(표시·미리보기 판정) |
| 업로드 중 모달에 포커스 갈 곳 0개 → 초점이 body 로, 탈출 불가 | 1회차 트랩을 패널 `onKeyDown` 에 걸어 body 에서 누른 Tab 이 버블하지 않음 | Tab 핸들러를 `document` 로, 갈 곳 0개면 패널 자신에 고정, MutationObserver 로 초점 이탈 복귀 |
| 모바일 «전체 선택» 체크박스 소멸 | 3차에 표 헤더를 `hidden … min-[631px]:flex` 로 감추며 그 안의 체크박스까지 사라짐 | 모바일 툴바에 복원 |
| 하위 «폴더» 차단에 하위 «자료»를 지우라는 안내 | 1회차 고아 가드가 서버 스킵과 같은 문구를 공유 | 사유 분리 — `drive-folder-del-has-subfolder` 신설 |
| 전건 성공에 「N개 중 N개«만»」 | 1회차 라이브 영역이 부분실패 문구를 무조건 사용 | 실패 0건이면 성공 문구 |
| 전건 성공 후 「등록」이 무반응 | 1회차에 성공 상태 분기를 안 만듦 | 전건 성공이면 「등록」을 렌더하지 않음 |

---

## 12. 미리보기 관련 지적은 제외 (사용자 결정)

3회차 검토의 다음 항목은 **미리보기 «기능»을 아직 만들지 않기로 한 결정**에 따라 제외한다.

| 지적 | 내용 | 처리 |
|---|---|---|
| M8 | 라이트박스 버튼 배치가 정본과 다르고 `N / M` 인덱스가 없다 | 제외 — 미리보기 기능 확장 시 함께 |
| H5 중 일부 | HWP·오피스 미리보기 | 제외 — §5 의 단계적 축소 결정 |

단, **H5 중 「`<video>`·`<iframe>` 이 공용 Modal 의 포커스 대상 목록에 없다」는 고쳤다.**
그것은 미리보기 «기능»이 아니라 **공용 `Modal` 의 결함**이고(어떤 모달이든 미디어·iframe 을 담으면
같은 문제가 난다), 2회차의 「라이트박스를 공용 Modal 로 이관」이 만든 회귀다.
`FOCUSABLE` 에 `iframe, video[controls], audio[controls]` 를 추가했다.

## 13. 3회차 검토 기록 (53건 → 컷 14 → 39건 → 병합 후 32건)

높음 6 · 중간 11 · 낮음 15. **높음 6건 중 5건이 1~2회차 수정의 회귀였다.**

### 자기 수정이 만든 회귀 (전부 수정)

| 회귀 | 원인 | 수정 |
|---|---|---|
| 성공 행을 상한에 이중 계상 → 올릴 수 있는데 「용량 부족」 | 2회차 `pending` 필터가 **코드에 적용되지 않았다**(치환 실패) | `rows.filter(status !== 'success')` |
| 겹친 모달의 document Tab 핸들러가 서로 초점을 되돌려 교착 | 2회차 «Tab 을 document 로 이관» 에 최상위 판정이 없었다 | 패널 스택 + `isTop()` 가드, MutationObserver 도 최상위만 |
| 진행 모달이 스크림 두 겹 아래로 가려짐 | 라이트박스와 진행 모달이 동시 마운트 | 미리보기에서 다운로드 시 **미리보기를 먼저 닫아 겹침을 제거** |
| 업로드 중 탈출 수단 0개(키보드 트랩) | `running` 이면 X·푸터를 전부 언마운트 | 닫기 버튼 유지(라벨 「닫기 (업로드는 계속됩니다)」) + ESC 우선순위 체인 |
| 미디어 컨트롤·PDF 본문에 초점 불가 | `FOCUSABLE` 에 `iframe`/`video` 누락 | 선택자 보강 |
| 삭제 확인이 폴더에도 「휴지통·복구 가능」을 약속 | 3차에 문구를 바꾸며 파일/폴더를 구분하지 않았고 개수 보간도 지웠다 | 파일/혼합 2분기 + 개수 보간 복원 |

### 문구 정정 — 폴더 삭제의 «영구 소멸» 범위

리포트의 「폴더 안의 휴지통 파일까지 영구 소멸」은 **단건 삭제 라우트** 기준이다
(`docs/api/08-drive-folder.md:224` — 하위 `FAIL`/`DEL` 파일을 DB hard delete).
우리는 **board 라우트**를 쓰고 그것은 `drive_folder_id = null` **고아 처리**다(`:283`) — 파일은 휴지통에 남는다.
다만 **폴더 복원 API 가 존재하지 않아**(`/drive/file/restore` 만 있다) 폴더 자체는 되돌릴 수 없다.
그래서 「복구 가능」 안내는 폴더에 대해 여전히 거짓이었고, 그 부분은 유효한 지적이었다.

### M11 — 상태 배경 위 전경색 토큰이 정본에 없다

`text-destructive` on `bg-destructive-bg` 는 라이트 2.82:1 · 다크 3.76:1 로 13px 본문 4.5:1 미달.
정본 토큰표에 «`*-bg` 위 전경색» 쌍이 정의돼 있지 않다(`l-*` 파스텔용 `--color-on-pastel` 만 있고,
그것은 테마 무관 고정이라 다크 틴트 배경에서 쓸 수 없다).

**토큰을 발명하지 않고** 색을 유일한 신호로 쓰지 않는 방향으로 해결했다 —
좌측 띠(`border-destructive`) + 아이콘이 «형태» 신호를 담당하고, 글자는 고대비 본문색(`text-gray-900`).
실측: 라이트 **15.0:1** · 다크 **11.72:1**.

**남은 과제**: 정본표에 `*-bg` 세트의 전경색 쌍을 정의하면 이 화면의 처리를 그 토큰으로 교체할 수 있다.

## 14. 검토 회차 요약

| 회차 | 올라옴 | 컷 | 최종 | 높음 | 그중 «앞 회차 수정의 회귀» |
|---|---:|---:|---:|---:|---|
| 1회차 | 51 | 13 | 34 | 2 | – |
| 2회차 | 55 | 13 | 42 | 2 | **2/2** |
| 3회차 | 53 | 14 | 32 | 6 | **5/6** |

**교훈 1 — 치환 결과를 확인하지 않으면 「고쳤다」가 거짓이 된다.**
2회차에 3건(`pending` 필터·라이브 영역 2건)을 「수정 완료」로 보고했으나 코드에 들어가지 않았다.
prettier 재포맷으로 문자열 치환이 실패했고 성공 메시지만 출력됐다. 이후 모든 치환에 `assert` 를 걸고
작업 후 grep 으로 전수 확인한다.

**교훈 2 — 접근성 장치는 «다른 장치와 겹칠 때»를 함께 설계해야 한다.**
포커스 트랩·초점 복귀·라이브 영역은 각각 단독으로는 맞았지만, 모달이 겹치거나 패널 내용이
런타임에 사라지는 순간 서로를 방해했다. 2·3회차 높음의 대부분이 이 유형이다.
