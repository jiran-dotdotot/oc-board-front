# 글쓰기 화면 — 디자인 수정 요청 프롬프트 (우리가 먼저 고친 항목)

디자인 프로젝트: Claude Design `1384f01c-020e-4f3b-bd5a-0ca5a21efdaf` 「전체 페이지 리뉴얼 계획」.
정본 = `개선안 통합 앱.dc.html`(웹) · `개선안 통합 앱 mobile.dc.html`(모바일). 아트보드 = `화면 05_글쓰기.dc.html`.
각 항목의 「요청 문구」를 디자인 프로젝트 채팅에 그대로 붙여 넣는다. 디자인 CLAUDE.md 규칙상 웹·모바일·아트보드 05 에 함께 반영해야 한다.

---

## #1 공개 범위 섹션·조직도 모달 제거 (아트보드 05)

- 대상: `화면 05_글쓰기.dc.html`(웹·모바일 정본에는 이미 없음)
- 현재 디자인: 발행 옵션에 「공개 범위(전체 공개/조직 지정) + 조직도에서 선택 → 조직도 모달」(A05 script `scope`·`orgChecks`·`orgOpen`)
- 우리가 구현한 것: 제거. 폼에 공개 범위 없음.
- 근거: 정본 웹·모바일 글쓰기 블록(web:624-815 · mobile:577-758)에 없음 · 레거시는 게시판 단위 공개(AddPostView.vue:92) · Go POST/PUT body 에 글 단위 공개범위 필드 없음(`docs/api/go/06-post-write.md:212-222`·`:272-286`).
- 요청 문구:
  > 화면 05_글쓰기 아트보드에서 「공개 범위」 옵션 행(전체 공개/조직 지정 라디오, 조직도에서 선택 버튼, 선택 칩)과 「공개 범위 선택」 조직도 모달을 제거해 주세요. 정본(개선안 통합 앱 web/mobile)의 글쓰기 폼에는 이미 없고, 서버 계약에도 글 단위 공개 범위가 없습니다. 공개 범위는 환경 설정의 게시판 단위 설정에만 남습니다.

## #2 본문 에디터 = 나모 iframe · 탭은 편집/미리보기 2개

- 대상: 웹·모바일 정본 + 아트보드 05
- 현재 디자인: 자체 툴바 12종(`wTools`) + textarea + 「편집 / HTML / 미리보기」 3탭(web:688-720)
- 우리가 구현한 것: 나모 CrossEditor iframe(툴바는 에디터 소유, 높이 580/356) + 하단 탭 「편집 | 미리보기」. HTML 탭 없음(나모가 자체 HTML 모드 제공).
- 근거: 사용자 결정(2026-09-10) — 상용 에디터 재사용, 새 패키지 0. 레거시 AddPostView.vue:443-475·:947-951.
- 요청 문구:
  > 글쓰기 본문 영역을 「외부 에디터 프레임」으로 바꿔 주세요. 우리 툴바를 그리지 말고 회색 상단 바(gray-50, 8px) 아래 580px(모바일 356px) 프레임 자리만 두고, 하단 탭은 「편집 | 미리보기」 2개로 줄여 주세요(HTML 탭 제거). 미리보기 탭은 본문 렌더 스타일(글 상세 본문과 동일)로 표시합니다.

## #3 게시판 잠금 문구의 조건

- 대상: 웹·모바일 정본(web:648-652 `wBoardLocked`) + 아트보드 05(`editBoardNote` 「…(발행 상태 ACT)」)
- 현재 디자인: 수정 모드면 항상 잠금 + 「수정 시 게시판은 변경할 수 없습니다.」
- 우리가 구현한 것: 원본 상태가 **SAVE(임시저장)** 이면 드롭다운 유지(이동 가능), 그 외(ACT/SCHEDULED/HIDE)만 잠금.
- 근거: Go `board_id` 이동은 원본 SAVE 에만 적용(`06-post-write.md:274`·`:300`). 레거시도 `:disable="isEdit && post?.state === 'ACT'"`(AddPostView.vue:613).
- 요청 문구:
  > 글 수정 모드의 게시판 잠금은 «발행(예약) 상태의 글»에만 적용해 주세요. 임시저장 글을 이어 쓸 때는 게시판 드롭다운이 열려 있어야 합니다(서버가 임시저장 글만 이동을 허용). 잠금 안내 문구는 그대로 두고, 노출 조건 주석을 「원본 상태 ≠ SAVE」로 바꿔 주세요.

## #4 공지 설정 노출 안내 문구

- 대상: 웹·모바일 정본(web:780 · mobile:727)
- 현재 디자인: 「※ 공지 설정은 게시판 관리자에게만 노출됩니다.」
- 우리가 구현한 것: 노출 조건 = 선택 게시판의 `can_manage`(게시판·카테고리·회사 관리자 OR). 문구를 「게시판·카테고리·회사 관리자」로 정정.
- 근거: Go badges 게이트 = CanManage(`06-post-write.md:228`), `BoardView.can_manage` 정의(`04-board.md:105`·`:57`).
- 요청 문구:
  > 글쓰기 「공지 설정」 안내 문구를 「※ 공지 설정은 게시판·카테고리·회사 관리자에게만 노출됩니다.」로 바꿔 주세요. 서버 권한 규칙(CanManage = 게시판 관리자 OR 카테고리 관리자 OR 회사 관리자)과 맞춥니다.

## #5 첨부·대표 이미지 「비활성(계약 대기)」 상태 시안

- 대상: 웹·모바일 정본(web:665-690 첨부 · :721-735 대표 이미지)
- 현재 디자인: 활성 상태만(내 PC·전체 삭제·드롭존·96px 박스)
- 우리가 구현한 것: 업로드 컨트롤 disabled(opacity 60%) + 안내 「첨부 업로드는 서버 계약 확정 후 제공됩니다. (BR-037)」. 수정 모드의 기존 첨부 목록·삭제, 기존 썸네일 표시·삭제는 활성.
- 근거: 게시글 첨부 업로드 계약 부재(`docs/api/backend-requests.md` BR-037; `06:224` 「별도 API」만 언급, `10:177` drive-uploads 는 DRIVE 전용 422).
- 요청 문구:
  > 글쓰기 첨부파일·대표 이미지 영역에 「비활성(서버 준비 중)」 상태를 추가해 주세요. 「내 PC」 버튼·드롭존·대표 이미지 추가 박스는 disabled(opacity .6, cursor not-allowed), 안내 줄은 한도 문구 대신 「첨부 업로드는 서버 계약 확정 후 제공됩니다.」로 바뀝니다. 수정 모드에서 기존 첨부 목록(삭제 X 버튼)과 기존 대표 이미지(삭제 버튼)는 활성으로 남습니다.

## #6 예약 시각 — 시간 칩 4개 + 임의 시각 입력

- 대상: 웹·모바일 정본 달력 모달(web:1648-1656 `calShowTime`)
- 현재 디자인: 시간 칩 09:00/10:00/14:00/18:00 만 + 「분은 5분 단위로 설정됩니다.」
- 우리가 구현한 것: 칩 4개(빠른 선택) + `<input type="time" step="300">`(임의 시각, 5분 단위).
- 근거: 「5분 단위」 문구와 칩 4개(정시만)가 모순. 레거시는 임의 시각 선택(vue-datepicker `hide-navigation=[]`, AddPostView.vue:1061) — 칩만이면 회귀.
- 요청 문구:
  > 예약 발행 달력 모달의 시간 줄에 빠른 선택 칩(09:00·10:00·14:00·18:00) 오른쪽으로 「시:분 직접 입력」 컨트롤(5분 단위)을 추가해 주세요. 「분은 5분 단위로 설정됩니다.」 안내는 이 입력에 대한 설명이 됩니다. 칩만 있으면 09:30 같은 시각을 고를 수 없습니다.

## #7 저장 오류 상태 시안

- 대상: 웹·모바일 정본 + 아트보드 05
- 현재 디자인: 인라인 에러는 「게시판을 선택해주세요.」·「제목을 입력해주세요.」 2종(web:657·665), 예약은 「과거 시각은 인라인 에러」 문구만.
- 우리가 구현한 것: 필드 인라인 에러 4종 추가 — 게시판(쓸 권한 없음 403 / 게시판 없음 404), 예약(예약 일시 필수 400 `POST_SCHEDULE_REQUIRED`, 과거 시각), 공지(종료일 역전 400 `BADGE_PERIOD_INVALID`, 공지 권한 없음 403) + 폼 상단 배너(작성자 아님 403 / 에디터 무응답 / 일반 실패).
- 근거: Go 검사 순서와 코드(`06-post-write.md:228`·`:292`·`:169-172`).
- 요청 문구:
  > 글쓰기 폼에 서버 오류 상태를 추가해 주세요. ① 게시판 필드 아래 인라인: 「이 게시판에 글을 쓸 권한이 없습니다.」 / 「게시판을 찾을 수 없습니다.」 ② 예약 일시 칩 아래: 「예약 발행 일시를 지정해주세요.」 / 「현재 이후 시각을 선택해주세요.」(칩 테두리 danger) ③ 공지 기간 칩 아래: 「공지 종료일은 시작일 이후여야 합니다.」 / 「이 게시판의 공지를 설정할 권한이 없습니다.」 ④ 폼 하단(버튼줄 위) danger-bg 배너: 「작성자만 수정할 수 있습니다.」 / 「에디터가 응답하지 않아 저장하지 못했습니다. 다시 시도해주세요.」 / 「저장에 실패했습니다.」

## #8 이탈 확인·수정 알림 모달을 정본에 포함

- 대상: 웹·모바일 정본
- 현재 디자인: 정본 파일은 256KiB 에서 잘려 이탈/알림 모달 마크업을 확인할 수 없음. 아트보드 05 에만 있음(`leaveOpen`·`notifyOpen`).
- 우리가 구현한 것: 아트보드 05 값 채택 — 이탈 「작성을 취소하시겠습니까?」/「작성 중인 내용은 저장되지 않습니다.」 계속 작성·나가기(dirty 일 때만), 수정 알림 「구성원에게 글이 수정되었다고 알림을 보내시겠습니까?」 보내지 않음·알림 보내기(발행된 글을 고칠 때만). 둘 다 공용 확인 모달(320px).
- 근거: 레거시 이탈 가드(router/index.ts:53-57)·수정 알림 `not_send_alarm`(AddPostView.vue:267-272), Go `not_send_alarm`(`06:219`).
- 요청 문구:
  > 정본(웹·모바일)에 글쓰기의 두 확인 모달을 넣어 주세요. ① 이탈 확인: 「작성을 취소하시겠습니까?」 부제 「작성 중인 내용은 저장되지 않습니다.」 버튼 [계속 작성][나가기] — 변경 사항이 있을 때만 뜹니다. ② 수정 알림: 발행된 글을 수정 저장할 때 「구성원에게 글이 수정되었다고 알림을 보내시겠습니까?」 버튼 [보내지 않음][알림 보내기]. 둘 다 공용 확인 모달(320px, 버튼 h40) 변형을 그대로 쓰면 됩니다.

## #9 프로토타입 값 정리 — 임시저장 카운트 배지·45% 진행률·고정 회사명

- 대상: 아트보드 05(`DRAFT_COUNT` 성격의 배지, 첨부 진행률 45%, 조직도 「지란지교소프트」)
- 현재 디자인: 데모용 고정값
- 우리가 구현한 것: 전부 옮기지 않음. 임시저장 개수는 서버가 별도로 주지 않고(`/posts/mine?state=SAVE` 의 `total` 로만 가능) 레거시에도 표시가 없어 배지 제거. 진행률은 업로드 계약 없음(#5).
- 요청 문구:
  > 화면 05 의 「임시 저장」 버튼 옆 카운트 배지, 첨부 목록의 45% 진행률 행, 조직도의 고정 회사명은 프로토타입 시드값이므로 정본에서 제외해 주세요. 진행률은 첨부 업로드 계약이 정해진 뒤 #5 의 활성 상태와 함께 다시 그립니다.
